version: '3'
services:
  db:
    restart: always
    image: "postgres:14.3-alpine"
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_USER=user
    volumes:
      - data:/var/lib/postgresql/data
    ports:
      - "1369:5432"
volumes:
  data:
